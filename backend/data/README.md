# Data folders

The real-data bootstrap populates `raw/` and `processed/` automatically. Generated files are intentionally not shipped in the ZIP so the project does not bundle stale or fabricated observations.
